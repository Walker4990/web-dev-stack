<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="m">

<select id="member_list" resultType="vo.MemberVO">
	select * from member
</select>
<insert id="register" parameterType="vo.MemberVO">
    INSERT INTO member (idx, name, userid, pwd, email, addr)
    VALUES (
     seq_member_idx.nextVal,
    #{name, jdbcType=VARCHAR},
    #{userid, jdbcType=VARCHAR},
    #{pwd, jdbcType=VARCHAR},
    #{email, jdbcType=VARCHAR},
    #{addr, jdbcType=VARCHAR})
    
</insert>
<delete id="member_del" parameterType="int">
	DELETE FROM member WHERE idx=#{idx}
</delete>
<update id="member_update" parameterType="java.util.Map">
   UPDATE member 
    SET name   = #{name,jdbcType=VARCHAR},
        userid = #{userid,jdbcType=VARCHAR},
        pwd    = #{pwd,jdbcType=VARCHAR},
        email  = #{email,jdbcType=VARCHAR},
        addr   = #{addr,jdbcType=VARCHAR}
    WHERE idx  = #{idx,jdbcType=INTEGER}
</update>

<select id="select_one" parameterType="int" resultType="vo.MemberVO">
	select * from member where idx=#{idx}
</select>
<select id="check_userid" parameterType="String" resultType="vo.MemberVO" >
	select * from member where userid = #{userid}
</select>
</mapper>












